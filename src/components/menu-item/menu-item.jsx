import React from 'react';
import PropTypes from 'prop-types';

const MenuItem = () => (
  <div className='jpt-menu-item'>MENU ITEM</div>
);

MenuItem.propTypes = {
  text: PropTypes.string,
};

export default MenuItem;
